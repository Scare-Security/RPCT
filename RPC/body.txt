
	package main // package name @{}@

	import (
		RPC_PRE main/Engine // Importing from the !{}! macro
	)


func Generate_LIA_Tag(values ...string) {
	var lias string
	for index, val := range values {
		lias += fmt.Sprintf("<li><a>%v</li></a>", val[index])
	}
	return string(lias)
}
func main() {for i,  k := range RPC_PRE.values  { fmt.Println(Generate_LIA_Tag(i[k])) }}